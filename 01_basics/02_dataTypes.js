"use strict"; // treat all code of JS as newer version

// alert(3 + 3); it will not work cause we are using node js, not browser
let age = 18  // int
let isLoggedIn = true // true ir false 
let name ="Akhtar" // string
let place; // undefined, value is not defined yet
let birth = null // standalone value
// symbol = unique  

// BigInt it also a datatype use in large number

console.log(typeof undefined);  // undefined
console.log(typeof null);   // object

